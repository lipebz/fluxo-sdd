# Ignore all local runs, logs, and temporary files generated by agents
*

# Keep this gitignore file tracked
!.gitignore

# Keep the commands directory and all its files tracked
!commands/
!commands/**
